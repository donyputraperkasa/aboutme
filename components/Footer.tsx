export default function Footer() {
    return (
        <footer className="mt-20 py-8 text-center text-gray-400 text-sm border-t border-white/10">
        <p>
            © {new Date().getFullYear()} dony putra Pprkasa — Software Engineer & Math Teacher
        </p>
        </footer>
    );
}